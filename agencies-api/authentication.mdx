---
title: Authentication
description: >-
  Authenticate requests to the Agencies Management API using a Supabase
  session JWT for an allowlisted admin account.
---

## Authentication

Every endpoint requires a Supabase session JWT for an allowlisted admin account, passed as a bearer token.

<Callout kind="alert">
  This is a **staff** credential, not an agency one. An agency key (`ak_`), a
  location secret key (`sk_`), or a scoped key (`mcp_`) is refused with `401`
  on every route in this section. There is no static admin key and no
  `?admin_key=` query parameter — a shared secret was retired precisely
  because it was unattributable and could not be revoked for one caller.
</Callout>

## Server-to-server callers

A backend that needs agency administration — creating agencies, linking
locations to them — is issued its own **service key** by Mythic, scoped to
exactly those routes:

```text
Authorization: Bearer <service key>
```

No token exchange and no refresh: the key is the credential. It is allowlisted
per route *and per method*, so a key that may read and update an agency cannot
delete one or rotate its key, and it is refused on every other `/api/v1` route.
Each key is separate, so its calls are attributable to it alone and it can be
revoked without affecting anyone else.

<Callout kind="note">
  A service key does **not** cover client management. Creating clients, install
  snippets, hostnames, and install verification are agency-grain and live on
  [`/client/v1/locations`](/locations-api/overview) behind the agency's own
  `ak_` key. If you are reaching for a service key to provision a client, that
  is the surface you want instead.
</Callout>

Contact Mythic to have one issued.

<ParamField header="Authorization" param-type="string" required="true" deprecated="false">
  Supabase session JWT for an allowlisted admin account. Format: `Bearer <access_token>`.
</ParamField>

<Callout kind="alert">
  Admin session tokens must never be exposed in client-side JavaScript, public repositories, or logs. Restrict access to trusted backend services and operations staff.
</Callout>

<Callout kind="info">
  The admin session that authenticates these requests is distinct from an agency's own `agency_key` (the `ak_` credential returned by these endpoints). The admin session authorizes management of agencies; the `agency_key` is used by client-facing APIs to identify a specific agency.
</Callout>
